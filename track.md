su-lee:
    fuzzing_dump: initial parameter set
    fuzzing_dump_redo: tried to sync creation of inlinable with defs and main_code

epyc5:
    fuzzing_dump: fuzzing with different, larger parameter set
    fuzzing_dump_consistent: fuzzing with epyc5:fuzzing_dump, except consistent walks were enabled
    random_init_set_1: random initialisations between -1000 and 1000 w su-lee's params
    random_init_set_1_consistent: random initialisations between -1000 and 1000 w su-lee's params and consistent walks enabled
    random_init_set_2_consistent: random initialisations between -1000 and 1000 w epyc5's params and consistent walks enabled
    random_init_set_3: random initialisations between -1000 and 1000 w larger params
    random_init_set_3_consistent: random initialisations between -1000 and 1000 w larger params and consistent walks enabled
    coeff_bound_set_1: coefficient bounding between -10 and 10 w su-lee's params
    
