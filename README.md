1. In the script `link.sh`, update the paths for gcc and clang
2. Please don't modify the existing directory structure
3. You can fine tune the parameters in params.hpp : my idea right now is to spawn multiple processes with different params and see if any could potentially trigger a bug
4. Post all these tweaks, we can