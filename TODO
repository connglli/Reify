C/C++:
2. the path sampling often leads to path with only 1 loop iteration.
4. support more conditions and bitwise ops
5. use yingwei/ubi and yingwei/csmith
6. pointers and memory
8. better optimization passes support, gvn/lvn still not working very well, consider intrinsics, idioms, and more slp patterns, separate functions from main so that hiding input parameters
9. better type system
10. sample multiple exec per function

Java:
1. rylink for java
2. separation compilation units

Interprocedural Dataflow: 
1. More Vectorization, LoopUnrolling, licm, InstCombine, ... Rules
2. Fuzzer could auto reduce bugs it finds or copy the original functions also into the bug dir.
   => The main issue is that regenerating functions is performance dependend (bitwuzla thread timeout)
      Hence it would be smarted to solved this during fuzz time rather then later
3. If/Else Guards
4. Add lowering for new structures (For mod arithmetic, new terms) Java and SYMIR as well as parsing for SYMIR (including Uninitialized Locals)
