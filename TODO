C/C++:
2. the path sampling often leads to path with only 1 loop iteration.
4. support more conditions and bitwise ops
5. use yingwei/ubi and yingwei/csmith
6. pointers and memory
8. better optimization passes support, gvn/lvn still not working very well, consider intrinsics, idioms, and more slp patterns, separate functions from main so that hiding input parameters
9. better type system
10. sample multiple exec per function

Java:
1. rylink for java
2. separation compilation units

Interprocedural Dataflow: 
1. Remove IfElse/For and While stmt and replace them with blocks
2. Adjust Rewrite Engine / Rules to work over list of blocks -> list of blocks rather then Block -> Block
3. More Vectorization, LoopUnrolling, licm, ... Rules
4. Expand Term to handle Bitwise ops
5. Add InstCombine Rules
6. Make Guard a Rewrite Rule rather then a seperate Operation -> Also needs Rules to see variable State
7. Add ability for Delta Debugging / Bisecting
8. If/Else Guards
9. Add lowering for new structures (For mod arithmetic) Java and SYMIR as well as parsing for SYMIR (including Uninitialized Locals)
10. turn FCallStrategy state like varMap, filtered{Variables, VarState, ...} etc into locals 

General: 
1. Bug in fuzzier python causes mass wrong flanging of whole programs when finding one faulty function
