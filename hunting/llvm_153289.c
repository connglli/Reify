int printf(const char *, ...);
int a, b, d, g, c, f;

int main() {
  int h, i, j;
  i = c;
  for (int e = 0; e < 1; e++)
    if (a)
      b = e;
  j = d + i - 1;
  g = printf("%d", j);
  h = j + 3;
  int k[] = {g + i - 1 + j, 0, f + j + i - 1, h + j + i + c};
  if (a)
    printf("%p", (void *) k);
  return 0;
}
